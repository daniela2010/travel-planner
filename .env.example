# Copy this file to .env and fill in the values before running locally.
# In production (Vercel), set REACT_APP_API_URL in the Vercel dashboard instead of this file.

# URL of the deployed backend (e.g. Render.com or Heroku)
# During local development this defaults to http://localhost:5000/api — leave blank.
REACT_APP_API_URL=
